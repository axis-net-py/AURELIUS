# Specification: AppShell FAB Enhancements

Enhance the Floating Action Button (FAB) in `AppShell` to improve AI visibility and accessibility.

## Acceptance Criteria

- FAB is visible on all screen sizes (added desktop visibility).
- Icon updated from `Send` to `Sparkles`.
- Changes verified with a successful build (`npm run build`).
- Changes are committed and pushed to remote.

## Files Impacted

- `src/components/layout/AppShell.tsx`
